#if !defined(MACRO_hpp)
#define MACRO_hpp

#define SOCKADDR(X) reinterpret_cast<sockaddr *>(&X)
#define BUFFER_SIZE 10
#define MAX_EVENT 20
#define WAITMS 10000
#define BACKLOG 10

#endif // MACRO_hpp
